(function(g) {
  function p(a) {
    function b(f) {
      var h = f.target;
      h = h.name || h.id || h.type;
      f = f.type;
      var i = a.name || a.id;
      _gas.push(["_trackEvent", "Form Tracking", "form" + (i ? " (" + i + ")" : ""), h + " (" + f + ")"])
    }
    var c, e;
    for(c in a.elements) {
      e = a.elements[c];
      ["button", "submit"].indexOf(e.type) >= 0 ? this._addEventListener(e, "click", b) : this._addEventListener(e, "change", b)
    }
    this._addEventListener(a, "submit", b)
  }
  function q() {
    j && clearTimeout(j);
    j = setTimeout(function() {
      var a = 0;
      if(typeof g.pageYOffset == "number") {
        a = g.pageYOffset
      }else {
        if(d.body && (d.body.scrollLeft || d.body.scrollTop)) {
          a = d.body.scrollTop
        }else {
          if(d.documentElement && (d.documentElement.scrollLeft || d.documentElement.scrollTop)) {
            a = d.documentElement.scrollTop
          }
        }
      }
      var b = 0;
      if(typeof g.innerWidth == "number") {
        b = g.innerHeight
      }else {
        if(d.documentElement && (d.documentElement.clientWidth || d.documentElement.clientHeight)) {
          b = d.documentElement.clientHeight
        }else {
          if(d.body && (d.body.clientWidth || d.body.clientHeight)) {
            b = d.body.clientHeight
          }
        }
      }
      var c = d;
      k = (a + b) / Math.max(Math.max(c.body.scrollHeight, c.documentElement.scrollHeight), Math.max(c.body.offsetHeight, c.documentElement.offsetHeight), Math.max(c.body.clientHeight, c.documentElement.clientHeight)) * 100
    }, 400)
  }
  function r() {
    this._addEventListener(g, "beforeunload", function() {
      var a = Math.floor(k / 10) * 10;
      if(a < 100) {
        a = String(a) + "-" + String(a + 9)
      }
      _gas.push(["_trackEvent", "Max Scroll", d.location.href, String(a), Math.round(k)])
    })
  }
  var d = g.document;
  g._gaq = g._gaq || [];
  var m = g._gas || [];
  if(!(m._accounts_length >= 0)) {
    var n = Object.prototype.hasOwnProperty, s = Array.prototype.slice, t = Array.prototype.indexOf, u = d.location.href;
    g._gas = {_accounts:{}, _accounts_length:0, _hooks:{}, _functions:{}, _queue:m};
    _gas._functions._addHook = function(a, b) {
      if(typeof a === "string" && typeof b === "function") {
        if(typeof _gas._hooks[a] === "undefined") {
          _gas._hooks[a] = []
        }
        _gas._hooks[a].push(b)
      }
    };
    _gas._functions._trackException = function(a, b) {
      _gas.push(["_trackEvent", "Exception " + (a.name || "Error"), b || a.message || a, u])
    };
    _gas._push_inner = function() {
      var a = s.call(arguments), b = a.shift(), c, e, f, h;
      if(typeof b === "function") {
        return _gaq.push(function(v) {
          return function() {
            v.call(_gas)
          }
        }(b))
      }else {
        if(typeof b === "object" && b.length > 0) {
          e = b.shift();
          if(t.call(e, ".") >= 0) {
            f = e.split(".")[0];
            e = e.split(".")[1]
          }
          if((a = _gas._hooks[e]) && a.length > 0) {
            for(c = 0;c < a.length;c++) {
              try {
                h = a[c].apply(_gas.gh, b);
                if(h === false) {
                  return 1
                }
                if(h && h.length > 0) {
                  b = h
                }
              }catch(i) {
                e !== "_trackException" && _gas.push(["_trackException", i])
              }
            }
          }
          if(n.call(_gas._functions, e)) {
            try {
              _gas._functions[e].apply(_gas.gh, b)
            }catch(w) {
              e !== "_trackException" && _gas.push(["_trackException", w])
            }
          }
          if(e === "_setAccount") {
            f = f || String(_gas._accounts_length + 1);
            _gas._accounts[f] = b[0];
            _gas._accounts_length++;
            return _gaq.push([f + "." + e, b[0]])
          }
          var o;
          for(c in _gas._accounts) {
            if(n.call(_gas._accounts, c)) {
              f = c + "." + e;
              a = b.slice();
              a.unshift(f);
              o += _gaq.push(a)
            }
          }
          return o ? 1 : 0
        }
      }
    };
    _gas.push = function() {
      (function(a) {
        _gaq.push(function() {
          _gas._push_inner.apply(_gas, a)
        })
      })(arguments)
    };
    var l = {};
    l._sanitizeString = function(a) {
      return a = a.toLowerCase().replace(/^\ +/, "").replace(/\ +$/, "").replace(/\s+/g, "_").replace(/[\u00e1\u00e0\u00e2\u00e3\u00e5\u00e4\u00e6\u00aa]/g, "a").replace(/[\u00e9\u00e8\u00ea\u00eb\u0404\u20ac]/g, "e").replace(/[\u00ed\u00ec\u00ee\u00ef]/g, "i").replace(/[\u00f3\u00f2\u00f4\u00f5\u00f6\u00f8\u00ba]/g, "o").replace(/[\u00fa\u00f9\u00fb\u00fc]/g, "u").replace(/[\u00e7\u00a2\u00a9]/g, "c")
    };
    l._addEventListener = function(a, b, c) {
      if(a.addEventListener) {
        a.addEventListener(b, c, false);
        return true
      }else {
        if(a.attachEvent) {
          return a.attachEvent("on" + b, c)
        }else {
          a["on" + b] = c
        }
      }
    };
    _gas.push(function() {
      var a = _gat._createTracker();
      (function(b) {
        for(var c in b) {
          c in this || (this[c] = b[c])
        }
      }).call(a, l);
      _gas.gh = a
    });
    if(typeof _gas._functions === "undefined") {
      _gas._functions = {}
    }
    _gas._functions._trackForms = function() {
      for(var a in d.forms) {
        p.call(this, d.forms[a])
      }
    };
    if(typeof _gas._functions === "undefined") {
      _gas._functions = {}
    }
    var j = null, k = 0;
    for(_gas._functions._trackMaxSrcoll = function() {
      this._addEventListener(g, "scroll", q);
      r.call(this)
    };_gas._queue.length > 0;) {
      _gas.push(_gas._queue.shift())
    }
    _gaq && _gaq.length >= 0 && function() {
      var a = d.createElement("script");
      a.type = "text/javascript";
      a.async = true;
      a.src = ("https:" == d.location.protocol ? "https://ssl" : "http://www") + ".google-analytics.com/ga.js";
      var b = d.getElementsByTagName("script")[0];
      b.parentNode.insertBefore(a, b)
    }()
  }
})(window);

