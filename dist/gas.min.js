(function(f, u) {
  function v(a) {
    function b(g) {
      var h = g.target;
      h = h.name || h.id || h.type;
      g = g.type;
      var j = a.name || a.id;
      _gas.push(["_trackEvent", "Form Tracking", "form" + (j ? " (" + j + ")" : ""), h + " (" + g + ")"])
    }
    var d, e;
    for(d in a.elements) {
      e = a.elements[d];
      ["button", "submit"].indexOf(e.type) >= 0 ? this._addEventListener(e, "click", b) : this._addEventListener(e, "change", b)
    }
    this._addEventListener(a, "submit", b)
  }
  function n() {
    var a = 0, b = 0;
    if(typeof f.innerWidth == "number") {
      a = f.innerWidth;
      b = f.innerHeight
    }else {
      if(c.documentElement && (c.documentElement.clientWidth || c.documentElement.clientHeight)) {
        a = c.documentElement.clientWidth;
        b = c.documentElement.clientHeight
      }else {
        if(c.body && (c.body.clientWidth || c.body.clientHeight)) {
          a = c.body.clientWidth;
          b = c.body.clientHeight
        }
      }
    }
    return[a, b]
  }
  function o() {
    var a = 0, b = 0;
    if(typeof f.pageYOffset == "number") {
      b = f.pageYOffset;
      a = f.pageXOffset
    }else {
      if(c.body && (c.body.scrollLeft || c.body.scrollTop)) {
        b = c.body.scrollTop;
        a = c.body.scrollLeft
      }else {
        if(c.documentElement && (c.documentElement.scrollLeft || c.documentElement.scrollTop)) {
          b = c.documentElement.scrollTop;
          a = c.documentElement.scrollLeft
        }
      }
    }
    return[a, b]
  }
  function p() {
    var a = c;
    return Math.max(Math.max(a.body.scrollHeight, a.documentElement.scrollHeight), Math.max(a.body.offsetHeight, a.documentElement.offsetHeight), Math.max(a.body.clientHeight, a.documentElement.clientHeight))
  }
  function q(a) {
    k && clearTimeout(k);
    if(a === true) {
      i = Math.max((o()[1] + n()[1]) / p() * 100, i)
    }else {
      k = setTimeout(function() {
        i = Math.max((o()[1] + n()[1]) / p() * 100, i)
      }, 400)
    }
  }
  function w() {
    this._addEventListener(f, "beforeunload", function() {
      q(true);
      var a = Math.floor(i / 10) * 10;
      if(a < 100) {
        a = String(a) + "-" + String(a + 9)
      }
      _gas.push(["_trackEvent", "Max Scroll", c.location.href, String(a), Math.round(i)])
    })
  }
  var c = f.document;
  f._gaq = f._gaq || [];
  var r = f._gas || [];
  if(!(r._accounts_length >= 0)) {
    var s = Object.prototype.hasOwnProperty, x = Array.prototype.slice, y = Array.prototype.indexOf, z = c.location.href;
    f._gas = {_accounts:{}, _accounts_length:0, _hooks:{}, _functions:{}, _queue:r};
    _gas._functions._addHook = function(a, b) {
      if(typeof a === "string" && typeof b === "function") {
        if(typeof _gas._hooks[a] === "undefined") {
          _gas._hooks[a] = []
        }
        _gas._hooks[a].push(b)
      }
    };
    _gas._functions._trackException = function(a, b) {
      _gas.push(["_trackEvent", "Exception " + (a.name || "Error"), b || a.message || a, z])
    };
    _gas._execute = function() {
      var a = x.call(arguments), b = a.shift(), d, e, g, h;
      if(typeof b === "function") {
        return _gaq.push(function(A) {
          return function() {
            A.call(_gas.gh)
          }
        }(b))
      }else {
        if(typeof b === "object" && b.length > 0) {
          e = b.shift();
          if(y.call(e, ".") >= 0) {
            g = e.split(".")[0];
            e = e.split(".")[1]
          }
          if((a = _gas._hooks[e]) && a.length > 0) {
            for(d = 0;d < a.length;d++) {
              try {
                h = a[d].apply(_gas.gh, b);
                if(h === false) {
                  return 1
                }
                if(h && h.length > 0) {
                  b = h
                }
              }catch(j) {
                e !== "_trackException" && _gas.push(["_trackException", j])
              }
            }
          }
          if(s.call(_gas._functions, e)) {
            try {
              _gas._functions[e].apply(_gas.gh, b)
            }catch(B) {
              e !== "_trackException" && _gas.push(["_trackException", B])
            }
          }
          if(e === "_setAccount") {
            g = g || String(_gas._accounts_length + 1);
            _gas._accounts[g] = b[0];
            _gas._accounts_length++;
            return _gaq.push([g + "." + e, b[0]])
          }
          var t;
          for(d in _gas._accounts) {
            if(s.call(_gas._accounts, d)) {
              g = d + "." + e;
              a = b.slice();
              a.unshift(g);
              t += _gaq.push(a)
            }
          }
          return t ? 1 : 0
        }
      }
    };
    _gas.push = function() {
      (function(a) {
        _gaq.push(function() {
          _gas._execute.apply(_gas, a)
        })
      })(arguments)
    };
    var l = {};
    l._sanitizeString = function(a, b) {
      a = a.toLowerCase().replace(/^\ +/, "").replace(/\ +$/, "").replace(/\s+/g, "_").replace(/[\u00e1\u00e0\u00e2\u00e3\u00e5\u00e4\u00e6\u00aa]/g, "a").replace(/[\u00e9\u00e8\u00ea\u00eb\u0404\u20ac]/g, "e").replace(/[\u00ed\u00ec\u00ee\u00ef]/g, "i").replace(/[\u00f3\u00f2\u00f4\u00f5\u00f6\u00f8\u00ba]/g, "o").replace(/[\u00fa\u00f9\u00fb\u00fc]/g, "u").replace(/[\u00e7\u00a2\u00a9]/g, "c");
      if(b) {
        a = a.replace(/[^a-z0-9_-]/g, "_")
      }
      return a.replace(/_+/g, "_")
    };
    l._addEventListener = function(a, b, d) {
      if(a.addEventListener) {
        a.addEventListener(b, d, false);
        return true
      }else {
        if(a.attachEvent) {
          return a.attachEvent("on" + b, d)
        }else {
          a["on" + b] = d
        }
      }
    };
    _gas.push(function() {
      var a = _gat._createTracker();
      (function(b) {
        for(var d in b) {
          d in this || (this[d] = b[d])
        }
      }).call(a, l);
      _gas.gh = a
    });
    if(typeof _gas._functions === "undefined") {
      _gas._functions = {}
    }
    _gas._functions._trackForms = function() {
      for(var a in c.forms) {
        v.call(this, c.forms[a])
      }
    };
    if(typeof _gas._functions === "undefined") {
      _gas._functions = {}
    }
    var k = null, i = 0;
    _gas._functions._trackMaxSrcoll = function() {
      this._addEventListener(f, "scroll", q);
      w.call(this)
    };
    if(typeof _gas._functions === "undefined") {
      _gas._functions = {}
    }
    _gas._allowAnchor = false;
    _gas.push(["_addHook", "_setAllowAnchor", function(a) {
      _gas._allowAnchor = a
    }]);
    _gas.push(["_addHook", "_getLinkerUrl", function(a, b) {
      if(b === u) {
        b = _gas._allowAnchor
      }
      return[a, b]
    }]);
    var m = [];
    _gas.push(["_addHook", "_setDomainName", function(a) {
      if(c.location.hostname.indexOf(a) < 0) {
        m.push(a);
        return false
      }
    }]);
    _gas.push(["_addHook", "_addExternalDomainName", function(a) {
      m.push(a);
      return false
    }]);
    _gas._functions._trackMultiDomain = function() {
      var a = c.location.hostname, b, d;
      for(b = 0;b < c.links.length;b++) {
        d = c.links[b];
        if(d.href.indexOf("http") == 0) {
          if(d.hostname != a) {
            if(d.hostname in m) {
              d.href = this._getLinkerUrl(d.href, _gas._allowAnchor)
            }
          }
        }
      }
    };
    for(;_gas._queue.length > 0;) {
      _gas.push(_gas._queue.shift())
    }
    _gaq && _gaq.length >= 0 && function() {
      var a = c.createElement("script");
      a.type = "text/javascript";
      a.async = true;
      a.src = ("https:" == c.location.protocol ? "https://ssl" : "http://www") + ".google-analytics.com/ga.js";
      var b = c.getElementsByTagName("script")[0];
      b.parentNode.insertBefore(a, b)
    }()
  }
})(window);

