(function(h, k) {
  function u(a) {
    function b(g) {
      var f = g.target;
      f = f.name || f.id || f.type;
      g = g.type;
      var i = a.name || a.id;
      _gas.push(["_trackEvent", "Form Tracking", "form" + (i ? " (" + i + ")" : ""), f + " (" + g + ")"])
    }
    var e, d;
    for(e in a.elements) {
      d = a.elements[e];
      ["button", "submit"].indexOf(d.type) >= 0 ? this._addEventListener(d, "click", b) : this._addEventListener(d, "change", b)
    }
    this._addEventListener(a, "submit", b)
  }
  function o() {
    var a = 0, b = 0;
    if(typeof h.innerWidth == "number") {
      a = h.innerWidth;
      b = h.innerHeight
    }else {
      if(c.documentElement && (c.documentElement.clientWidth || c.documentElement.clientHeight)) {
        a = c.documentElement.clientWidth;
        b = c.documentElement.clientHeight
      }else {
        if(c.body && (c.body.clientWidth || c.body.clientHeight)) {
          a = c.body.clientWidth;
          b = c.body.clientHeight
        }
      }
    }
    return[a, b]
  }
  function p() {
    var a = 0, b = 0;
    if(typeof h.pageYOffset == "number") {
      b = h.pageYOffset;
      a = h.pageXOffset
    }else {
      if(c.body && (c.body.scrollLeft || c.body.scrollTop)) {
        b = c.body.scrollTop;
        a = c.body.scrollLeft
      }else {
        if(c.documentElement && (c.documentElement.scrollLeft || c.documentElement.scrollTop)) {
          b = c.documentElement.scrollTop;
          a = c.documentElement.scrollLeft
        }
      }
    }
    return[a, b]
  }
  function q() {
    var a = c;
    return Math.max(Math.max(a.body.scrollHeight, a.documentElement.scrollHeight), Math.max(a.body.offsetHeight, a.documentElement.offsetHeight), Math.max(a.body.clientHeight, a.documentElement.clientHeight))
  }
  function r(a) {
    l && clearTimeout(l);
    if(a === true) {
      j = Math.max((p()[1] + o()[1]) / q() * 100, j)
    }else {
      l = setTimeout(function() {
        j = Math.max((p()[1] + o()[1]) / q() * 100, j)
      }, 400)
    }
  }
  function v() {
    this._addEventListener(h, "beforeunload", function() {
      r(true);
      var a = Math.floor(j / 10) * 10;
      if(a < 100) {
        a = String(a) + "-" + String(a + 9)
      }
      _gas.push(["_trackEvent", "Max Scroll", s, String(a), Math.round(j)])
    })
  }
  var c = h.document;
  h._gaq = h._gaq || [];
  var t = h._gas || [];
  if(!(t._accounts_length >= 0)) {
    var w = Object.prototype.hasOwnProperty, x = Array.prototype.slice, y = Array.prototype.indexOf, s = c.location.href;
    h._gas = {_accounts:{}, _accounts_length:0, _hooks:{}, _queue:t, gh:{}};
    _gas._hooks._addHook = [function(a, b) {
      if(typeof a === "string" && typeof b === "function") {
        if(typeof _gas._hooks[a] === "undefined") {
          _gas._hooks[a] = []
        }
        _gas._hooks[a].push(b)
      }
      return false
    }];
    _gas._execute = function() {
      var a = x.call(arguments), b = a.shift();
      a = true;
      var e, d, g, f, i;
      if(typeof b === "function") {
        return _gaq.push(function(z) {
          return function() {
            z.call(_gas.gh)
          }
        }(b))
      }else {
        if(typeof b === "object" && b.length > 0) {
          d = b.shift();
          if(y.call(d, ".") >= 0) {
            f = d.split(".")[0];
            d = d.split(".")[1]
          }else {
            f = k
          }
          if((g = _gas._hooks[d]) && g.length > 0) {
            for(e = 0;e < g.length;e++) {
              try {
                i = g[e].apply(_gas.gh, b);
                if(i === false) {
                  a = false
                }
                if(i && i.length > 0) {
                  b = i
                }
              }catch(A) {
                d !== "_trackException" && _gas.push(["_trackException", A])
              }
            }
          }
          if(a === false) {
            return 1
          }
          if(d === "_setAccount") {
            f = f || "_gas" + String(_gas._accounts_length + 1);
            _gas._accounts[f] = b[0];
            _gas._accounts_length++;
            return _gaq.push([f + "." + d, b[0]])
          }
          if(f && _gas._accounts[f]) {
            i = f + "." + d;
            a = b.slice();
            a.unshift(i);
            return _gaq.push(a)
          }
          g = 0;
          for(e in _gas._accounts) {
            if(w.call(_gas._accounts, e)) {
              i = e + "." + d;
              a = b.slice();
              a.unshift(i);
              g += _gaq.push(a)
            }
          }
          return g ? 1 : 0
        }
      }
    };
    _gas.push = function() {
      (function(a) {
        _gaq.push(function() {
          _gas._execute.apply(_gas.gh, a)
        })
      })(arguments)
    };
    _gas.push(["_addHook", "_trackException", function(a, b) {
      _gas.push(["_trackEvent", "Exception " + (a.name || "Error"), b || a.message || a, s]);
      return false
    }]);
    _gas.push(["_addHook", "_popHook", function(a) {
      (a = _gas._hooks[a]) && a.pop && a.pop();
      return false
    }]);
    var m = {};
    m._sanitizeString = function(a, b) {
      a = a.toLowerCase().replace(/^\ +/, "").replace(/\ +$/, "").replace(/\s+/g, "_").replace(/[\u00e1\u00e0\u00e2\u00e3\u00e5\u00e4\u00e6\u00aa]/g, "a").replace(/[\u00e9\u00e8\u00ea\u00eb\u0404\u20ac]/g, "e").replace(/[\u00ed\u00ec\u00ee\u00ef]/g, "i").replace(/[\u00f3\u00f2\u00f4\u00f5\u00f6\u00f8\u00ba]/g, "o").replace(/[\u00fa\u00f9\u00fb\u00fc]/g, "u").replace(/[\u00e7\u00a2\u00a9]/g, "c");
      if(b) {
        a = a.replace(/[^a-z0-9_-]/g, "_")
      }
      return a.replace(/_+/g, "_")
    };
    m._addEventListener = function(a, b, e) {
      if(a.addEventListener) {
        a.addEventListener(b, e, false);
        return true
      }else {
        if(a.attachEvent) {
          return a.attachEvent("on" + b, e)
        }else {
          b = "on" + b;
          if(typeof a[b] === "function") {
            e = function(d, g) {
              return function() {
                d.apply(this, arguments);
                g.apply(this, arguments)
              }
            }(a[b], e)
          }
          a[b] = e;
          return true
        }
      }
    };
    _gas.push(function() {
      var a = _gat._createTracker();
      (function(b) {
        for(var e in b) {
          e in this || (this[e] = b[e])
        }
      }).call(a, m);
      _gas.gh = a
    });
    _gas.push(["_addHook", "_trackForms", function() {
      for(var a in c.forms) {
        u.call(this, c.forms[a])
      }
      return false
    }]);
    var l = null, j = 0;
    _gas.push(["_addHook", "_trackMaxSrcoll", function() {
      this._addEventListener(h, "scroll", r);
      v.call(this)
    }]);
    _gas._allowAnchor = false;
    _gas.push(["_addHook", "_setAllowAnchor", function(a) {
      _gas._allowAnchor = a
    }]);
    _gas.push(["_addHook", "_getLinkerUrl", function(a, b) {
      if(b === k) {
        b = _gas._allowAnchor
      }
      return[a, b]
    }]);
    _gas.push(["_addHook", "_link", function(a, b) {
      if(b === k) {
        b = _gas._allowAnchor
      }
      return[a, b]
    }]);
    _gas.push(["_addHook", "_linkByPost", function(a, b) {
      if(b === k) {
        b = _gas._allowAnchor
      }
      return[a, b]
    }]);
    var n = [];
    _gas.push(["_addHook", "_setDomainName", function(a) {
      if(c.location.hostname.indexOf(a) < 0) {
        n.push(a);
        return false
      }
    }]);
    _gas.push(["_addHook", "_addExternalDomainName", function(a) {
      n.push(a);
      return false
    }]);
    _gas.push(["_addHook", "_setMultiDomain", function(a) {
      var b = c.location.hostname, e, d;
      if(a !== "now" || a !== "mousedown") {
        a = "click"
      }
      for(e = 0;e < c.links.length;e++) {
        d = c.links[e];
        if(d.href.indexOf("http") == 0) {
          if(d.hostname != b) {
            if(d.hostname in n) {
              if(a === "now") {
                d.href = this._getLinkerUrl(d.href, _gas._allowAnchor)
              }else {
                this._addEventListener(d, a, function() {
                  this.href = this._getLinkerUrl(this.href, _gas._allowAnchor)
                })
              }
            }
          }
        }
      }
      return false
    }]);
    for(_gas.push(["_setMultiDomain", "now"]);_gas._queue.length > 0;) {
      _gas.push(_gas._queue.shift())
    }
    _gaq && _gaq.length >= 0 && function() {
      var a = c.createElement("script");
      a.type = "text/javascript";
      a.async = true;
      a.src = ("https:" == c.location.protocol ? "https://ssl" : "http://www") + ".google-analytics.com/ga.js";
      var b = c.getElementsByTagName("script")[0];
      b.parentNode.insertBefore(a, b)
    }()
  }
})(window);

